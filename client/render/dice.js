/**
 * Dead of Winter — Dice Renderer
 * Phase 5: Rapier physics, procedural faces, camera switch.
 * Phase 1: Stub module.
 */

export function initDice (scene, store) {
  // Implemented in Phase 5
  return {
    throwDice () { return Promise.resolve([]) },
    reset () {}
  }
}
